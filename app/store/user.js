const cleanBotkitDataToModel = botkitData => ({
  ...botkitData,
});

const cleanModelDataToBotkit = modelData => ({
  ...modelData,
});

module.exports = {
  get: async (id, cb) => {
  },
  save: async (rawData, cb) => {
  },
  delete(id, cb) {
  },
  all(cb) {
  },
};
